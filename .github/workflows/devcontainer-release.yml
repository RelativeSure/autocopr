name: "Release Dev Container Templates & Generate Documentation"

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-24.04
    permissions:
      packages: write
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2 
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and run Dev Container task
        uses: devcontainers/ci@v0.3
        with:
          # Change this to point to your image name
          imageName: fedora-python
          subFolder: .devcontainer
          # Change this to be your CI task/script
          # runCmd: |
          #   # Add multiple commands to run if needed
          #   make install-packages
          #   make ci-build

      # - name: Dev Container Build and Run Action
      #   uses: devcontainers/ci@a56d055efecd725e8cfe370543b6071b79989cc8 # v0.3.1900000349
      #   with:
      #     imageName: ghcr.io/${{ github.repository }}/fedora-python
      #     push: always
      #     checkoutPath: .
      #     subFolder: .devcontainer
      #     eventFilterForPush: push
      #     skipContainerUserIdUpdate: false
      #     noCache: false

      # - name: Push to GitHub Packages
      #   run: |
      #     docker tag fedora-python:latest ghcr.io/${{ github.repository }}/fedora-python:latest
      #     docker push ghcr.io/${{ github.repository }}/fedora-python:latest
