name: Setup Fedora GH Actions image

on: [push]

jobs:
  publish-gh-actions-image:
    runs-on: ubuntu-24.04
    permissions:
      packages: write
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: "{{defaultContext}}:.github"
          push: true
          tags: ghcr.io/relativesure/gh-actions-image:0.0.2

      #- name: Build the GH Actions Docker image
      #  run: |
      #  docker build . --tag ghcr.io/relativesure/gh-actions-image:v0.1
      #  docker run ghcr.io/relativesure/gh-actions-image:v0.1
      #  docker push ghcr.io/relativesure/gh-actions-image:v0.1
