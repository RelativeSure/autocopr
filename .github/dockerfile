FROM fedora:41

# Upgrade all packages
RUN dnf -y upgrade --refresh

RUN dnf -y install curl wget python-copr python-copr-doc \ 
  dnf-plugins-core git && dnf clean all

# Set bash as the default shell
ENTRYPOINT ["/bin/bash"]
